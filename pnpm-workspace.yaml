packages:
  - apps/*
  - packages/*
allowBuilds:
  '@parcel/watcher': true
  # AppKit's optional version-reporting script is not needed at runtime.
  '@reown/appkit': false
  '@swc/core': true
  esbuild: true
  # The distributed package is prebuilt; postinstall only prints version advice.
  protobufjs: false
